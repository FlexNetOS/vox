name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

env:
  CARGO_TERM_COLOR: always

jobs:
  test:
    name: ${{ matrix.name }}
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - name: macOS (Metal)
            os: macos-latest
            features: metal
            deps: ""
          - name: Linux (CPU)
            os: ubuntu-latest
            features: ""
            deps: libasound2-dev
          - name: Linux (CUDA)
            os: ubuntu-latest
            features: cuda
            deps: libasound2-dev
            cuda: true
          - name: Windows (CPU)
            os: windows-latest
            features: ""
            deps: ""
          - name: Windows (CUDA)
            os: windows-latest
            features: cuda
            cuda: true
            deps: ""

    steps:
      - uses: actions/checkout@v4

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Install system deps (Linux)
        if: matrix.deps != ''
        run: sudo apt-get update && sudo apt-get install -y ${{ matrix.deps }}

      - name: Install CUDA toolkit (Linux)
        if: matrix.cuda == true && runner.os == 'Linux'
        shell: bash
        run: |
          wget -q https://developer.download.nvidia.com/compute/cuda/repos/ubuntu2404/x86_64/cuda-keyring_1.1-1_all.deb
          sudo dpkg -i cuda-keyring_1.1-1_all.deb
          sudo apt-get update -q
          sudo apt-get install -y --no-install-recommends cuda-toolkit-12-6
          echo "/usr/local/cuda/bin" >> "$GITHUB_PATH"
          echo "CUDA_PATH=/usr/local/cuda" >> "$GITHUB_ENV"

      - name: Install CUDA toolkit (Windows)
        if: matrix.cuda == true && runner.os == 'Windows'
        uses: Jimver/cuda-toolkit@v0.2.22
        with:
          cuda: '12.6.3'
          method: network

      - name: Set up MSVC environment (Windows CUDA)
        if: matrix.cuda == true && runner.os == 'Windows'
        uses: ilammy/msvc-dev-cmd@v1

      - name: Cache cargo
        uses: actions/cache@v4
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            target
          key: ${{ runner.os }}-${{ matrix.features }}-cargo-${{ hashFiles('**/Cargo.lock') }}

      - name: Build
        run: cargo build ${{ matrix.features != '' && format('--features {0}', matrix.features) || '' }}
        env:
          CUDA_COMPUTE_CAP: ${{ matrix.cuda == true && '89' || '' }}

      - name: Run tests
        run: cargo test ${{ matrix.features != '' && format('--features {0}', matrix.features) || '' }}
        env:
          CUDA_COMPUTE_CAP: ${{ matrix.cuda == true && '89' || '' }}

      - name: Check formatting
        if: matrix.cuda != true
        run: cargo fmt -- --check

      - name: Clippy
        run: cargo clippy ${{ matrix.features != '' && format('--features {0}', matrix.features) || '' }} -- -D warnings -A clippy::collapsible_if -A clippy::collapsible_match
        env:
          CUDA_COMPUTE_CAP: ${{ matrix.cuda == true && '89' || '' }}
